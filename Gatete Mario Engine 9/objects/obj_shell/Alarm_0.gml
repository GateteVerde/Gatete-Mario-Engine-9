/// @description Set offset

//If there's a koopa inside the shell
if (koopainside != 0) {

	//Repeat the process
	alarm[0] = 1;
	
	//Set up offset
	if (offset != 1)
        offset = 1;
    else
        offset = -1;
}