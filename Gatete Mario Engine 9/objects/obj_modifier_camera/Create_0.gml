/// @description Map Camera Modifier Setup

camera_x = 0;
camera_y = 0;

camera_x_min = x;
camera_x_max = x + (sprite_get_width(sprite_index) * image_xscale);

camera_y_min = y;
camera_y_max = y + (sprite_get_height(sprite_index) * image_yscale);