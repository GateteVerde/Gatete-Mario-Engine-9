/// @description Prepare a football

//If Mario does exist...
if (instance_exists(obj_mario)) {
    
    //Draw the ball
    ready = 1;
        
    //Generate it
    alarm[1] = 30;
}
else
    alarm[0] = 1;