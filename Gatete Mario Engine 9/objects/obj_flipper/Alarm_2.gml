/// @description Change frame and make the object solid

//Set the frame
image_index = 0;

//Make the flipper able to open
ready = 0;

//Make the flipper solid on contact
with (mysolid) {

    x = xstart;
    y = ystart;
}