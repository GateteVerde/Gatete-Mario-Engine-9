/// @description Move into place, and come down a bit

//Snap in position
x = camera_get_view_x(view_camera[0]) + camera_get_view_width(view_camera[0]) - 72;

//Move down a bit
yspeed = 0.5
alarm[1] = 64;